# topic-classification-model
주제분류모델 테스트

정글에서 spreadout 진행할 때 각기 다른 글의 내용 유사도를 측정하는 모델들을 테스트할 때 사용했던 레포

관련 라이브러리 설치
pip install PyMuPDF scikit-learn
pip install konlpy JPype1
*자바 설치 필요

pip install matplotlib
pip install seaborn
pip install numpy
